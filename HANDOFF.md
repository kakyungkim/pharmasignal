# 이어서 볼 것

> 2026-08-23 새벽 작업. 자고 일어나서 보실 때 여기부터 읽으시면 됩니다.

## 어젯밤에 한 것

신규성 조사(`research/`) 결과를 코드에 반영했습니다. 세 주장 모두 선행이 있었고,
조사가 짚은 결함 다섯 중 넷을 메웠습니다.

| 지적 | 조치 | 파일 |
|---|---|---|
| 이상사례 데이터를 한 건도 안 봄 | ROR 불균형 분석 추가 | `code/src/pharmasignal/signal.py` |
| 민감도 분류기도 강제도 없음 | 상용 경로에 관문 설치 | `code/src/pharmasignal/guard.py` |
| 대조 커버리지를 모름 | 커버리지 지표, 스폰서 포함, 실패 처리 | `verify.py` |
| 원장이 덮어씀 | 덧붙이기 + 해시 | `ledger.py` |
| 계열 정의가 검색어 매칭 | **미해결.** 분모를 드러내는 것으로 대신함 | - |

그리고 조사가 지목한 **흰 공간을 실제로 재는 명령**을 만들었습니다.

```bash
pharmasignal measure --runs 3
```

## 확인해 주셨으면 하는 것

**첫째, `docs/10-honest-positioning.md`를 봐 주세요.** 무엇이 선행이고 무엇이 남는지
정리한 문서입니다. 주장의 범위를 좁혔는데, 너무 깎았다고 보시면 말씀해 주세요.

**둘째, 측정 결과입니다.** `docs/11-measurement.md`에 있습니다(밤사이 완료되면).
불일치율이 0에 가까우면 대조 단계가 형식적 장치라는 뜻이라, 그 해석을 어떻게 쓸지가
판단이 필요한 대목입니다.

**셋째, 설명 영상을 다시 굽지 않았습니다.** 나레이션 문구가 바뀌었는데
(Nosana 표현, 대조가 확립된 기법이라는 문장) 영상은 옛 나레이션 그대로입니다.
아래 명령으로 다시 구우시면 됩니다. 5분쯤 걸립니다.

```bash
cd video/explain && zsh build.sh
```

## 남은 과제

- **계열 정의를 검색어에서 MeSH나 개입 약물 코드로.** "antibody-drug conjugate"가
  "conjugate vaccine"을 끌어오는 문제가 그대로입니다.
- **웹툰.** 벤치마킹할 것을 찾아 주신다고 하셔서 보류 중입니다.
  `webtoon/`에 만들다 만 6컷이 있고 저장소에는 올리지 않았습니다.
- **Ryan 등(2013)의 참조 세트로 AUC 측정.** 조사가 제안한 다음 단계입니다.
  양성 대조 165개, 음성 대조 234개로 외부 눈금에 올라설 수 있습니다.

## 현재 상태

테스트 28개, 게이트 4/4, 실호출 4/4. 저장소는 최신 상태로 푸시돼 있습니다.
